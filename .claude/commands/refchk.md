---
description: Cross-check a proposed implementation against the reference codebases. Read-only.
---

For the file or function the user references: find the equivalent pattern in `refs/stakpak/` (and `refs/claude-code/` if relevant).

Use the `reference-explorer` sub-agent for the actual lookup.

Compare. Highlight any meaningful differences. Suggest whether Terrashift's approach is consistent with the reference or whether a deviation should be documented.

If a deviation is intentional, point to the constitution article that governs it (often Article XI for amendments, or Article II for explicit non-adoption per terrashift_plan.md §16's "what we don't adopt" lists).
