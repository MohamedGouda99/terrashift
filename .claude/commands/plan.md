---
description: Query the Terrashift architecture plan. Quote relevant sections when answering. Read-only.
---

Read `terrashift_plan.md`. Answer questions about the architecture, stages, or constitution. Quote the relevant section when responding.

If the question requires reading reference codebases:
- Use the `reference-explorer` sub-agent.
- Reference paths resolve via `pre-flight.md` decision 2 (`~/refs/stakpak/` → `C:\Users\goda\Desktop\agent\` etc).

Do not modify the plan unless explicitly asked. Modifications are an Article XI amendment requiring RFC + team consensus.
