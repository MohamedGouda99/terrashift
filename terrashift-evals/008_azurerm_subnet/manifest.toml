name = "008_azurerm_subnet"
description = "Azure subnet with virtual_network_name Reference; exercises required-via-Reference on Azure-side networking"
source_provider = "aws"
target_provider = "azurerm"
token_cost_ceiling_micros = 0
articles = [3, 6, 9]
required_schemas = [
  { provider = "azurerm", version = "3.110.0" },
]
