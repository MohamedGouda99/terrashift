name = "002_aws_subnet_with_reference"
description = "AWS VPC + subnet with vpc_id Reference; exercises raw-HCL traversal emission"
source_provider = "google"
target_provider = "aws"
token_cost_ceiling_micros = 0
articles = [3, 6, 9]
required_schemas = [
  { provider = "aws", version = "5.30.0" },
]
