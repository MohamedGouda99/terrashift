name = "018_google_network_to_azurerm_vnet"
description = "GCP Compute Network → Azure VNet; closes the google-source → azurerm-target gap (PR #25 matrix proved this direction works with real LLM; this golden makes it a CI gate)"
source_provider = "google"
target_provider = "azurerm"
token_cost_ceiling_micros = 0
articles = [3, 6, 8]
required_schemas = [
  { provider = "azurerm", version = "3.110.0" },
]
