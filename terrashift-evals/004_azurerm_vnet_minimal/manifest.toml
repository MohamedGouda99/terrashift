name = "004_azurerm_vnet_minimal"
description = "AWS VPC → Azure Virtual Network; exercises the AWS→Azure direction (Pratik fixture path)"
source_provider = "aws"
target_provider = "azurerm"
token_cost_ceiling_micros = 0
articles = [3, 6, 9]
required_schemas = [
  { provider = "azurerm", version = "3.110.0" },
]
