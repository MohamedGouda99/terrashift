name = "017_google_storage_to_aws_s3"
description = "GCP Storage Bucket → AWS S3 Bucket; closes the google-source coverage gap (PR #25 matrix proved this direction works with real LLM; this golden makes it a CI gate)"
source_provider = "google"
target_provider = "aws"
token_cost_ceiling_micros = 0
articles = [3, 6, 8]
required_schemas = [
  { provider = "aws", version = "5.30.0" },
]
