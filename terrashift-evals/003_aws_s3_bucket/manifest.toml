name = "003_aws_s3_bucket"
description = "Single AWS S3 bucket; exercises a non-VPC resource family"
source_provider = "google"
target_provider = "aws"
token_cost_ceiling_micros = 0
articles = [3, 6, 9]
required_schemas = [
  { provider = "aws", version = "5.30.0" },
]
