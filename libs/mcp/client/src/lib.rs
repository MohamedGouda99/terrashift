//! MCP client built on rmcp.
//!
//! Pattern: stakpak_arch.md section 13 (MCP suite — config + client + server + proxy).
//! Constitution: Article V (mTLS by default), Article XIII rule 5.

#[cfg(test)]
mod tests {
    #[test]
    fn crate_compiles() {}
}
